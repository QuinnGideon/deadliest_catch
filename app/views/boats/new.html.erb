<%= form_for @boat do |f| %>
	<%= f.text_field :name %> 
	<%= f.text_field :capacity %> 
	<%= f.text_field :location %> 
	<%= f.hidden_field :user_id, value: current_user.id %>
	<%= f.submit %> 
<% end %>